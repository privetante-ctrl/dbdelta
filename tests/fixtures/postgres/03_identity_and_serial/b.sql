CREATE TABLE a (id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY);
CREATE TABLE b (id integer GENERATED ALWAYS AS IDENTITY PRIMARY KEY);
CREATE TABLE c (id integer PRIMARY KEY);
CREATE TABLE d (id bigserial PRIMARY KEY);
